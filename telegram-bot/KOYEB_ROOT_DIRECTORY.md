# Как указать папку для деплоя в Koyeb

## ⚠️ Важно: Root Directory может отсутствовать

В текущем интерфейсе Koyeb (app.koyeb.com/services/) поле **Root Directory** может отсутствовать в настройках. Это нормально! Используйте альтернативный способ через команды.

## 📍 Способ 1: Через команды (рекомендуется, если нет Root Directory)

### При создании сервиса:

1. Перейдите на https://app.koyeb.com/services/
2. Нажмите **"Create Service"**
3. Выберите GitHub репозиторий
4. В поле **"Build Command"** укажите:
   ```
   cd telegram-bot && npm install && npm run build
   ```
5. В поле **"Run Command"** укажите:
   ```
   cd telegram-bot && npm start
   ```

### Если бот в корне репозитория:
- Build Command: `npm install && npm run build`
- Run Command: `npm start`

## 📍 Способ 2: Через Root Directory (если поле есть)

### При создании сервиса:

1. После выбора GitHub репозитория прокрутите форму вниз
2. Найдите раздел **"Build & Deploy Settings"** или **"Source"**
3. Если есть поле **"Root Directory"**, введите:
   ```
   telegram-bot
   ```
4. В этом случае команды могут быть без `cd`:
   - Build Command: `npm install && npm run build`
   - Run Command: `npm start`

### В настройках существующего сервиса:

1. Откройте сервис на https://app.koyeb.com/services/
2. Перейдите в **"Settings"**
3. Найдите раздел **"Source"** или **"Build & Deploy"**
4. Если есть поле **"Root Directory"**, измените на `telegram-bot`
5. Нажмите **"Save"**

## 🖼️ Визуальное расположение

```
Create App
├── Select Repository (GitHub)
├── Basic Settings
│   ├── Name: telegram-bot
│   └── Region: Frankfurt
│
└── Build & Deploy Settings  ← ИЩИТЕ ЗДЕСЬ
    ├── Root Directory: telegram-bot  ← ВОТ ОНО!
    ├── Build Command: npm install && npm run build
    └── Run Command: npm start
```

## ✅ Рекомендуемый способ (актуальный на 2024)

**Используйте команды с `cd`** - это работает всегда, независимо от наличия поля Root Directory:

### Build Command:
```bash
cd telegram-bot && npm install && npm run build
```

### Run Command:
```bash
cd telegram-bot && npm start
```

Это самый надежный способ, который работает в любом интерфейсе Koyeb.

## ✅ Проверка правильности настройки

После деплоя проверьте логи. Если все правильно, вы увидите:

```
> npm install
...
> npm run build
...
> npm start
🤖 Telegram bot started successfully!
```

Если видите ошибку типа "package.json not found", значит Root Directory указан неправильно.

## 🔍 Примеры для разных структур репозитория

### Структура 1: Бот в подпапке
```
repository/
├── telegram-bot/
│   ├── package.json
│   ├── src/
│   └── ...
└── README.md
```
**Root Directory**: `telegram-bot`

### Структура 2: Бот в корне
```
repository/
├── package.json
├── src/
└── ...
```
**Root Directory**: `.` или оставить пустым

### Структура 3: Бот в глубокой вложенности
```
repository/
├── projects/
│   └── telegram-bot/
│       ├── package.json
│       └── ...
```
**Root Directory**: `projects/telegram-bot`

## 💡 Советы

1. **Путь относительный**: Указывайте путь относительно корня репозитория
2. **Без слешей**: Не используйте `/telegram-bot`, просто `telegram-bot`
3. **Регистр важен**: Убедитесь, что регистр букв совпадает с реальной папкой
4. **Проверка**: После деплоя проверьте логи - если видите "package.json not found", путь неправильный

