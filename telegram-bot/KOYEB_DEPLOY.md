# Подробная инструкция по деплою на Koyeb

Koyeb - отличный выбор для Telegram бота: бесплатный тариф, не "засыпает", работает круглосуточно, и **НЕ требует привязки карты**.

**🔍 Не можете найти Root Directory?** См. раздел ниже с подробными инструкциями.

## 📋 Подготовка

1. Убедитесь, что ваш код находится в GitHub репозитории
2. Проверьте, что все переменные окружения готовы

## 🚀 Пошаговая инструкция

### Шаг 1: Создание аккаунта

1. Перейдите на https://www.koyeb.com
2. Нажмите "Sign up" (Регистрация)
3. Выберите способ регистрации:
   - Через GitHub (рекомендуется)
   - Через Email
4. Завершите регистрацию

### Шаг 2: Создание нового приложения

1. После входа в аккаунт нажмите кнопку **"Create App"** или **"New App"**
2. Выберите **"GitHub"** как источник кода
3. Если это первый раз, авторизуйте Koyeb для доступа к вашим GitHub репозиториям
4. Выберите репозиторий с вашим ботом

### Шаг 3: Настройка приложения

После выбора репозитория вы увидите форму настройки. Заполните следующие поля:

#### Основные настройки:
- **Name**: `telegram-bot` (или любое другое имя)
- **Region**: Выберите ближайший регион (например, `Frankfurt` для Европы)

#### Build & Deploy настройки:

**⚠️ Важно: Root Directory может отсутствовать в интерфейсе!**

В текущем интерфейсе Koyeb поле **Root Directory** может отсутствовать. Это нормально! Используйте команды с `cd`:

1. **Build Command** (рекомендуется): 
   ```
   cd telegram-bot && npm install && npm run build
   ```
   Если бот в корне репозитория: `npm install && npm run build`

2. **Run Command** (рекомендуется): 
   ```
   cd telegram-bot && npm start
   ```
   Если бот в корне репозитория: `npm start`

**Альтернатива (если поле Root Directory есть):**
- Найдите поле **"Root Directory"** в разделе **"Build & Deploy Settings"** или **"Source"**
- Введите: `telegram-bot`
- В этом случае команды могут быть без `cd`:
  - Build Command: `npm install && npm run build`
  - Run Command: `npm start`

#### Environment настройки:
- **Environment**: `Node.js`
- **Node Version**: `18` (или выше)

**💡 Где найти Root Directory:**
- После выбора репозитория, в форме настройки прокрутите вниз
- Ищите раздел **"Build & Deploy"** или **"Source Configuration"**
- Поле может называться:
  - `Root Directory`
  - `Working Directory`
  - `Source Directory`
  - `Base Directory`

### Шаг 4: Добавление переменных окружения

1. В форме настройки найдите раздел **"Environment Variables"** или **"Secrets"**
2. Добавьте следующие переменные:

   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   FIREBASE_PROJECT_ID=ваш_project_id
   FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
   FIREBASE_CLIENT_EMAIL=ваш_service_account_email
   NODE_ENV=production
   ```

2. **Важно для FIREBASE_PRIVATE_KEY**:
   - **Способ 1 (рекомендуется)**: Оберните значение в кавычки `"` и сохраните `\n`:
     ```
     FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n"
     ```
   - **Способ 2 (альтернатива)**: Без кавычек, но с `\n`:
     ```
     FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n
     ```
   - Код автоматически обработает оба формата
   - Главное: сохраните все `\n` (не заменяйте на реальные переносы строк)

3. Нажмите **"Add Variable"** для каждой переменной

### Шаг 5: Деплой

1. После настройки всех параметров нажмите **"Deploy"** или **"Create Service"**
2. Koyeb начнет процесс деплоя:
   - Клонирует репозиторий
   - Перейдет в указанную папку (если использовали `cd telegram-bot`)
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Запустит бота (`npm start`)
3. Процесс займет 2-5 минут
4. После завершения статус изменится на **"Live"**

### Шаг 6: Проверка деплоя

1. После завершения деплоя вы увидите статус **"Live"**
2. Перейдите в раздел **"Logs"** чтобы проверить логи
3. Вы должны увидеть сообщение:
   ```
   🤖 Telegram bot started successfully!
   🌐 Health check server listening on port 3000
   ```

## 🔍 Мониторинг и логи

### Просмотр логов:
1. В панели управления приложением перейдите в раздел **"Logs"**
2. Вы увидите все логи в реальном времени
3. Можно фильтровать по уровню (Info, Error, etc.)

### Проверка статуса:
- **"Live"** - приложение работает
- **"Building"** - идет сборка
- **"Deploying"** - идет деплой
- **"Error"** - произошла ошибка

## 🔄 Автоматический деплой

Koyeb автоматически деплоит при каждом push в основную ветку (обычно `main` или `master`).

### Настройка автоматического деплоя:
1. По умолчанию включен автоматический деплой
2. Можно настроить в разделе **"Settings" → "Deploy"**
3. Можно выбрать конкретную ветку для деплоя

## 🛠️ Обновление приложения

### Способ 1: Автоматический (рекомендуется)
1. Внесите изменения в код
2. Сделайте commit и push в GitHub
3. Koyeb автоматически задеплоит новую версию

### Способ 2: Ручной деплой
1. В панели управления нажмите **"Redeploy"**
2. Выберите коммит для деплоя
3. Нажмите **"Deploy"**

## ⚙️ Обновление переменных окружения

1. Перейдите в раздел **"Settings" → "Environment Variables"**
2. Измените нужные переменные
3. Нажмите **"Save"**
4. Приложение автоматически перезапустится с новыми переменными

## 🐛 Решение проблем

### Проблема: Бот не запускается

**Решение:**
1. Проверьте логи в разделе **"Logs"**
2. Убедитесь, что все переменные окружения установлены правильно
3. Проверьте, что `TELEGRAM_BOT_TOKEN` указан верно
4. Убедитесь, что `FIREBASE_PRIVATE_KEY` обернут в кавычки и содержит `\n`

### Проблема: Ошибка "DECODER routines::unsupported" или "Getting metadata from plugin failed"

**Это ошибка с Firebase приватным ключом. Решение:**

1. **Проверьте формат FIREBASE_PRIVATE_KEY в Koyeb:**
   - Откройте Settings → Environment Variables
   - Убедитесь, что ключ обернут в кавычки: `"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"`
   - Убедитесь, что все `\n` присутствуют (не заменены на реальные переносы строк)

2. **Попробуйте пересоздать переменную:**
   - Удалите старую переменную `FIREBASE_PRIVATE_KEY`
   - Создайте новую с правильным форматом
   - Скопируйте ключ из JSON файла Firebase полностью
   - Оберните в кавычки и сохраните все `\n`

3. **Альтернативный формат (без кавычек):**
   ```
   FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n
   ```

4. **После изменения переменной:**
   - Сохраните изменения
   - Дождитесь автоматического перезапуска сервиса
   - Проверьте логи - должно появиться: `✅ Firebase Admin initialized successfully`

### Проблема: Ошибка сборки

**Решение:**
1. Проверьте команды Build и Run:
   - Если бот в папке `telegram-bot` → используйте `cd telegram-bot && ...`
   - Если бот в корне репозитория → используйте команды без `cd`
2. Убедитесь, что `package.json` находится в правильной папке
3. Проверьте логи сборки на наличие ошибок (раздел "Logs")
4. Убедитесь, что путь в команде `cd` указан правильно

### Проблема: "package.json not found"

**Решение:**
Это означает, что команды выполняются не в той папке. Исправьте:

**Если бот в папке `telegram-bot`:**
- Build Command: `cd telegram-bot && npm install && npm run build`
- Run Command: `cd telegram-bot && npm start`

**Если бот в корне репозитория:**
- Build Command: `npm install && npm run build`
- Run Command: `npm start`

### Проблема: Не могу найти поле Root Directory

**Решение:**
В текущем интерфейсе Koyeb (app.koyeb.com/services/) поле Root Directory может отсутствовать. Это нормально! Используйте команды с `cd`:

**Build Command:**
```
cd telegram-bot && npm install && npm run build
```

**Run Command:**
```
cd telegram-bot && npm start
```

Это эквивалентно указанию Root Directory.

### Проблема: "HTTP health check failed on port 3000" или "TCP health check failed"

**Решение:**

1. **Проверьте логи** - должно быть сообщение:
   ```
   🌐 Health check server listening on 0.0.0.0:3000
   ✅ Health check available at http://0.0.0.0:3000/health
   ```
   Если его нет, значит сервер не запустился.

2. **Настройте Health Check в Koyeb:**
   - Откройте Settings → найдите раздел **"Health Check"** или **"Health"**
   - **Port**: `3000`
   - **Path**: `/health` (рекомендуется) или `/`
   - **Type**: `HTTP`
   - **Method**: `GET` (не POST!)
   - **Interval**: `10s` (если доступно)
   - **Timeout**: `5s` (если доступно)

3. **Проверьте переменную окружения PORT:**
   - В Environment Variables добавьте: `PORT=3000`
   - Это гарантирует, что сервер запустится на правильном порту

4. **Важно:**
   - Health check сервер запускается **ДО** бота, поэтому должен работать даже если бот не запустился
   - Используйте метод **GET**, не POST
   - Путь `/health` работает, также работает `/`

5. **Если проблема сохраняется:**
   - Подождите 2-3 минуты после деплоя
   - Проверьте логи - сервер должен запуститься первым
   - Попробуйте отключить health check временно (если есть опция)
   - Или измените тип на "None" / "Disabled"

6. **Проверка вручную (если доступен URL):**
   - После деплоя Koyeb должен предоставить URL сервиса
   - Попробуйте открыть: `https://your-service-url.koyeb.app/health`
   - Должно вернуться: `OK`

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи на наличие ошибок
2. Убедитесь, что токен бота правильный
3. Проверьте, что Firebase credentials корректны
4. Попробуйте перезапустить приложение через **"Redeploy"**

## 📊 Ограничения бесплатного тарифа

- **RAM**: 512MB (достаточно для бота)
- **CPU**: Shared (достаточно для бота)
- **Трафик**: Неограниченный
- **Хранилище**: 1GB
- **Приложения**: Неограниченное количество

## 💡 Полезные советы

1. **Используйте GitHub для хранения кода** - это упрощает деплой
2. **Не коммитьте `.env` файл** - используйте переменные окружения в Koyeb
3. **Проверяйте логи регулярно** - это поможет быстро находить проблемы
4. **Используйте health check** - бот уже настроен на `/health` endpoint

## 🔗 Полезные ссылки

- [Koyeb Documentation](https://www.koyeb.com/docs)
- [Koyeb Status](https://status.koyeb.com)
- [Koyeb Community](https://www.koyeb.com/community)

## ✅ Чеклист перед деплоем

- [ ] Код загружен в GitHub
- [ ] Все переменные окружения подготовлены
- [ ] `package.json` содержит правильные скрипты
- [ ] `tsconfig.json` настроен правильно
- [ ] `.env` файл НЕ закоммичен в репозиторий
- [ ] Токен бота получен от @BotFather
- [ ] Firebase credentials готовы

## 🎉 Готово!

После успешного деплоя ваш бот будет работать круглосуточно на Koyeb бесплатно!

