# Telegram Bot для записи к парикмахеру

Telegram бот для управления записями к парикмахеру с интеграцией в Firebase Firestore.

## Возможности

- 🔐 Авторизация через номер телефона
- 📝 Запись на прием к специалисту
- 📋 Просмотр будущих и прошлых записей
- ❌ Отмена записей
- 🎨 Минималистичный и удобный интерфейс

## Требования

- Node.js 18+
- Firebase проект с Firestore
- Telegram Bot Token от @BotFather

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните переменные окружения в `.env`:
   - `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
   - `FIREBASE_PROJECT_ID` - ID вашего Firebase проекта
   - `FIREBASE_PRIVATE_KEY` - приватный ключ сервисного аккаунта Firebase
   - `FIREBASE_CLIENT_EMAIL` - email сервисного аккаунта Firebase

## Получение учетных данных Firebase

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект
3. Перейдите в "Project Settings" → "Service Accounts"
4. Нажмите "Generate new private key"
5. Скачанный JSON файл содержит необходимые данные:
   - `project_id` → `FIREBASE_PROJECT_ID`
   - `private_key` → `FIREBASE_PRIVATE_KEY` (скопируйте полностью, включая `\n`)
   - `client_email` → `FIREBASE_CLIENT_EMAIL`

## Запуск

### Локальная разработка

```bash
npm run dev
```

### Production

```bash
npm run build
npm start
```

## Деплой

### Railway.app (первые 30 дней бесплатно)

1. Создайте аккаунт на [Railway.app](https://railway.app/)
2. Создайте новый проект и подключите GitHub репозиторий
3. Укажите root директорию: `telegram-bot`
4. Добавьте переменные окружения в настройках проекта
5. Railway автоматически определит Node.js проект и запустит бота

### Альтернативные бесплатные варианты

**Рекомендуется**: Fly.io или Render.com

Подробные инструкции по деплою на различные платформы смотрите в файле [DEPLOYMENT_OPTIONS.md](./DEPLOYMENT_OPTIONS.md)

**Быстрый старт с Fly.io** (не "засыпает", работает круглосуточно):

```bash
# Установите flyctl
iwr https://fly.io/install.ps1 -useb | iex

# Войдите
fly auth login

# Создайте приложение
cd telegram-bot
fly launch

# Добавьте переменные окружения
fly secrets set TELEGRAM_BOT_TOKEN=your_token
fly secrets set FIREBASE_PROJECT_ID=your_project_id
fly secrets set FIREBASE_PRIVATE_KEY="your_key"
fly secrets set FIREBASE_CLIENT_EMAIL=your_email

# Деплой
fly deploy
```

## Структура проекта

```
telegram-bot/
├── src/
│   ├── index.ts              # Точка входа
│   ├── bot.ts                # Основная логика бота
│   ├── handlers/             # Обработчики команд и действий
│   ├── services/             # Сервисы для работы с Firebase
│   └── types/                # TypeScript типы
├── package.json
├── tsconfig.json
└── README.md
```

## Использование

1. Откройте бота в Telegram
2. Отправьте команду `/start`
3. Отправьте контакт (номер телефона)
4. Если ваш номер найден в базе данных, вы увидите главное меню
5. Если номер не найден, следуйте инструкциям для регистрации на сайте

## Примечания

- Номер телефона должен быть в формате базы данных (8XXXXXXXXXX)
- Бот работает только с пользователями, зарегистрированными на сайте
- Записи синхронизируются с веб-приложением через Firebase

